# detection assets

This directory is no longer an active ROS 2 detection workspace.

The authoritative perception workspace is:

```bash
modules/perception/detection_ws/src
```

The remaining files under `modules/detection` are retained only as model/script assets for compatibility with older runbooks. Do not build or source `modules/detection/detection_ws`; that workspace has been removed from the active package graph.
